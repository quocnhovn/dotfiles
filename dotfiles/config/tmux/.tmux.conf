# Tmux Configuration for Development Workflow
# Enhanced with best practices from dotfiles-docs.vercel.app

# Change prefix key to Ctrl-a (more comfortable than Ctrl-b)
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Reload configuration file
bind r source-file ~/.tmux.conf \; display-message "Config reloaded!"

# Enable mouse support
set -g mouse on

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Automatically renumber windows when one is closed
set -g renumber-windows on

# Increase scrollback buffer size
set -g history-limit 50000

# Enable 256 colors and true color support
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*256col*:Tc"
set -ga terminal-overrides '*:Ss=\E[%p1%d q:Se=\E[ q'
set-environment -g COLORTERM "truecolor"

# Reduce command delay
set -sg escape-time 0

# Enable activity alerts
setw -g monitor-activity on
set -g visual-activity on

# Set window title
set -g set-titles on
set -g set-titles-string "#T"

# Window splitting with more intuitive keys
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"

# Pane navigation with vim-like keys
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Pane resizing
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# Window navigation
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5
bind -n M-6 select-window -t 6
bind -n M-7 select-window -t 7
bind -n M-8 select-window -t 8
bind -n M-9 select-window -t 9

# Copy mode settings (vim-like)
setw -g mode-keys vi
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "xclip -in -selection clipboard"

# Quick window switching
bind -n M-h previous-window
bind -n M-l next-window

# Session management
bind S command-prompt -p "New session name:" "new-session -s '%%'"
bind K confirm-before -p "Kill session? (y/n)" kill-session

# Pane synchronization toggle
bind e setw synchronize-panes

# Clear screen and history
bind C-l send-keys 'C-l'
bind C-k send-keys -R \; clear-history

# Status bar configuration (Catppuccin theme will override these)
set -g status on
set -g status-interval 1
set -g status-justify centre
set -g status-position bottom

# These will be managed by Catppuccin theme
# Status bar colors
# set -g status-bg colour235
# set -g status-fg colour255

# Status bar left side (Catppuccin will handle)
# set -g status-left-length 40
# set -g status-left "#[fg=colour39,bg=colour234,bold] #S #[fg=colour234,bg=colour235,nobold]"

# Status bar right side (Catppuccin will handle)
# set -g status-right-length 150
# set -g status-right "#[fg=colour246,bg=colour235] %Y-%m-%d %H:%M #[fg=colour39,bg=colour234,bold] #h "

# Window status format (Catppuccin will handle)
# setw -g window-status-format "#[fg=colour246,bg=colour235] #I:#W "
# setw -g window-status-current-format "#[fg=colour235,bg=colour39,bold] #I:#W #[fg=colour39,bg=colour235,nobold]"

# Pane border colors (Catppuccin will handle)
# set -g pane-border-style fg=colour235
# set -g pane-active-border-style fg=colour39

# Message colors (Catppuccin will handle)
# set -g message-style fg=colour255,bg=colour235,bold

# Clock mode
setw -g clock-mode-colour colour39

# Development workspace setup
bind D source-file ~/.tmux/dev-session

# Quick commands for development
bind -n M-t new-window -n "terminal" -c "#{pane_current_path}"
bind -n M-e new-window -n "editor" -c "#{pane_current_path}" "nvim"
bind -n M-g new-window -n "git" -c "#{pane_current_path}" "git status"
bind -n M-d new-window -n "docker" "docker ps"

# Laravel Artisan shortcuts
bind A command-prompt -p "Artisan command:" "new-window -n 'artisan' 'php artisan %%'"

# Docker shortcuts
bind C-d new-window -n "docker-compose" "docker-compose up"

# Git shortcuts
bind G new-window -n "lazygit" "lazygit"

# Plugins (TPM - Tmux Plugin Manager)
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'catppuccin/tmux'
set -g @plugin 'Morantron/tmux-fingers'

# Catppuccin theme configuration
set -g @catppuccin_flavour 'macchiato'
set -g @catppuccin_window_left_separator ""
set -g @catppuccin_window_right_separator " "
set -g @catppuccin_window_middle_separator " █"
set -g @catppuccin_window_number_position "right"
set -g @catppuccin_window_default_fill "number"
set -g @catppuccin_window_default_text "#W"
set -g @catppuccin_window_current_fill "number"
set -g @catppuccin_window_current_text "#W"
set -g @catppuccin_status_modules_right "directory user host session"
set -g @catppuccin_status_left_separator  " "
set -g @catppuccin_status_right_separator ""
set -g @catppuccin_status_fill "icon"
set -g @catppuccin_status_connect_separator "no"
set -g @catppuccin_directory_text "#{pane_current_path}"

# Tmux Fingers configuration
set -g @fingers-key f
set -g @fingers-compact-hints 0
set -g @fingers-hint-format "#[fg=red,bold]%s"
set -g @fingers-highlight-format "#[fg=yellow,bold,dim]%s"

# Plugin settings
set -g @resurrect-strategy-nvim 'session'
set -g @resurrect-capture-pane-contents 'on'
set -g @continuum-restore 'on'
set -g @continuum-save-interval '15'

# Copy to system clipboard
set -g @yank_selection 'clipboard'
set -g @yank_selection_mouse 'clipboard'

# Search highlighting
set -g @copycat_search_C-f '^.*$'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
